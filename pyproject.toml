[project]
name = "research-store"
version = "0.1.0"
description = "Read-only PDF to Markdown research store for Codex"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "markitdown[pdf]>=0.1.4",
]

[project.scripts]
research-store = "research_store.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/research_store"]

