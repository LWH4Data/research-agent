[store]
documents = "knowledge/documents"
conversations = "knowledge/conversations"
state = ".research-store/state.json"
temporary = ".research-store/tmp"

# Put PDFs in the local papers directory. External absolute paths also work.
[[sources]]
id = "papers"
path = "papers"
